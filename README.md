# My Cellar v0.1

A working Vite/React web app for a private wine cellar.

## Deploy
Upload these files to GitHub, then import the repo into Vercel. Framework: Vite. Build command: `npm run build`. Output directory: `dist`.
